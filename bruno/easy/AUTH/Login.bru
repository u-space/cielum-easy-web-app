meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{baseCoreUrl}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "username": "matiuno+admin@gmail.com",
    "password": "Contraseña"
  }
}

vars:post-response {
  token: res.body
}
